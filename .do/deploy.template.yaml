name: sample-fastify-basic-auth
services:
- environment_slug: node-js
  github:
    branch: main
    repo: bojand/sample-fastify-basic-auth
  http_port: 8080
  instance_count: 1
  instance_size_slug: basic-xxs
  name: sample-fastify-basic-auth
  routes:
  - path: /
  run_command: node index.js
